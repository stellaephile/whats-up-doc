import React from 'react';
import ReactDOM from 'react-dom/client';
import AppWithSymptoms from './AppWithSymptoms';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <AppWithSymptoms />
  </React.StrictMode>
);